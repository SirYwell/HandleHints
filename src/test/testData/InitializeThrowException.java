import java.lang.invoke.MethodHandle;
import java.lang.invoke.MethodHandles;

class InitializeThrowException {
  <info descr="(IllegalAccessError)double">private static final MethodHandle MH = <info descr="(IllegalAccessError)double">MethodHandles.throwException(double.class, IllegalAccessError.class)</info>;</info>
}